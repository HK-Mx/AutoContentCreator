# ============================================================================
# MVP REQUIREMENTS (Phase 1: Trends + Scripts + Dashboard + Email)
# Phase 2+ adds: Video, Publishing, Database persistence
# ============================================================================

# Core & Async
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
fastapi==0.104.1
uvicorn==0.24.0
httpx>=0.23.0,<1
aiohttp==3.9.1

# LLMs & AI (Tavily + Gemini only for MVP)
# Note: anthropic disabled in MVP (using Gemini only)
# Note: ollama removed (will add in Phase 2 if needed for scoring)
anthropic==0.7.0
google-generativeai==0.3.0
tavily-python==0.3.0

# Monitoring & Logging
python-json-logger==2.0.7
sentry-sdk==1.39.0
prometheus-client==0.19.0

# Testing & Utils
pytest==7.4.0
pytest-asyncio==0.21.0
pytz==2023.3
tenacity==8.2.3
pyyaml==6.0.1

# ============================================================================
# PHASE 2+ (Cuando facturemos):
# - Audio & Video: elevenlabs, moviepy, librosa, ffmpeg-python
# - Database: supabase, boto3, sqlalchemy, psycopg2-binary
# - APIs: tweepy, instagrapi, google-auth
# ============================================================================
